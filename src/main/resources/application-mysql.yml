spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/evidencelab?useUnicode=true&characterEncoding=utf8&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai}
    username: ${DB_USERNAME:evidencelab}
    password: ${DB_PASSWORD:evidencelab}
    driver-class-name: com.mysql.cj.jdbc.Driver
